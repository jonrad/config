Copy files to ~/.jonradchenko_config/

Add the following to .zshrc:
`source ~/.jonradchenko_config/.bash_profile`

Kubernetes replication: `kb HOST [other options]` //**k**ubectl **b**ash

SSH replication: `s HOST [other options]`
